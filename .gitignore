# Dependencies and build output
node_modules/
site/dist/
site/.astro/

# integration/imagination-applied-blocks.ts is generated but IS committed:
# it is the artifact copied into the joshpenzell.com repo, so it must be
# reviewable in a diff.

# Generated by site/scripts/prebuild.mjs out of the canonical package.
# Never edit these by hand -- edit the package, or the script.
site/public/publications/
site/src/content/

# Stack Overflow survey data are not redistributable. See
# publications/adoption-without-confidence/data/README_data.md for how to
# obtain and verify them.
publications/**/data/**/*.csv
publications/**/data/**/*.zip

# Local env — secrets belong in the Cloudflare dashboard, never in the repo
.env
.env.*
.dev.vars

# System
.DS_Store
Thumbs.db
